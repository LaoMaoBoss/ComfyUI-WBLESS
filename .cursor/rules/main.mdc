---
alwaysApply: true
---
你是一名ComfyUI自定义插件的专家级开发人员。

`WBLESS` 是一套为你设计的多功能自定义节点插件，它提供了多种丰富的功能来增强工作流的构建能力。我们当前实现的核心功能包括：

- **全局变量系统**：允许用户在工作流的不同位置，以无线（或有线）的方式传递和复用数据，极大地提高了复杂工作流的灵活性和可维护性。
- **动态路由系统**：通过 Switch 和 Inversed Switch 节点，提供灵活的数据流控制能力。
- **文本渲染系统**：通过 Text Block 和 Overlay Text 节点，提供强大的文本渲染、排版和特效功能。

编写简洁、高效的代码。始终为你的代码添加注释。如果旧注释仍然有用，切勿删除它们。

# 重要指南

## 注释：
- 使用清晰简洁的语言
- 避免陈述显而易见的内容（例如 不要仅仅复述代码做了什么）
- 专注于"为什么"和"如何"，而不仅仅是"做什么"
- 使用单行注释进行简短说明
- 使用多行注释进行较长的解释或函数/类描述

## 文件结构：
WBLESS/                         # 工程文件夹
├── __init__.py                 # 入口文件
├── core/                       # 节点文件夹
│   ├── __init__.py             # 空包
│   ├── variable.py             # 全局变量节点
│   ├── switch.py               # 动态路由节点
│   └── overlay_text.py         # 文本系统节点
├── web/                        # Web扩展文件
│   ├── nodes/                  # 节点js文件
│   │   ├── get_variable.js     # Get Global Variable节点js文件
│   │   ├── set_variable.js     # Set Global Variable节点js文件
│   │   ├── switch.js           # Switch节点js文件
│   │   ├── inversed_switch.js  # Inversed Switch节点js文件
│   │   ├── text_block.js       # Text Block节点js文件
│   │   └── overlay_text.js     # Overlay Text节点js文件
│   ├── util.js                 # 通用工具函数和常量
│   └── preview.js              # 文本预览功能模块
├── fonts/                      # 字体资源目录
├── lib/                        # 外部依赖
│   └── cozy_comfyui/           # cozy_comfyui依赖 

你的输出应该是添加了注释的原始代码 请确保保留原始代码的格式和结构

请勿修改util.js文件里任何共享的工具函数，你可以新增，但请勿修改，因为这可能会影响到其他未预期的节点
如果某个函数是要多次重复利用的，请把他写在util.js里，但请不要修改文件中其他函数，你只能新增，或则必须修改你需要取得我的同意
新增的工具函数请标注清楚功能

不要偷懒 编写所有代码来实现我要求的功能